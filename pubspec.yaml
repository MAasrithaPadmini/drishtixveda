name: drishtixveda
description: "DrishtiXVeda Flutter App"
publish_to: 'none'

version: 1.0.1+2

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:

  flutter:
    sdk: flutter
  syncfusion_flutter_charts: ^26.1.41
  url_launcher: ^6.2.5
  http: ^1.2.0
  image_picker: ^1.0.7
  # webview_flutter: ^4.4.2
  #uni_links: ^0.5.1
  app_links: ^3.5.0
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.0
  firebase_messaging: ^14.7.10

  cupertino_icons: ^1.0.8
  fl_chart: ^1.2.0
  cloud_firestore: ^4.17.5
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
